const {
  song_detail,
  playlist_track_all,
  search,
  lyric
} = require('./NeteaseCloudMusicApi-master/main')
var fs = require("fs")

async function search_list(sid) {
  try {
    
  } catch (error) {
    console.log(error);
  }
}
